// Bismillah
#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int T;
    cin >> T;
    while (T--)
    {
        float n;
        cin >> n;

        cout << floor(n / 2) << endl;
        // cout<<__gcd(2,2)<<endl;
        // for (int i = 0; i < n; i++)
        // {

        // }
    }
    return 0;
}
// Alhamdulillah