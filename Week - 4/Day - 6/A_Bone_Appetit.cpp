// Bismillah
#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{
    ll a, b, x, y;
    cin >> a >> b;
    cin >> x >> y;
    ll sum = (a * x) + (b * y);
    cout << sum;
    return 0;
}
// Alhamdulillah